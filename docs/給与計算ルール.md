# 給与計算ルール（労務士確定・恒久仕様）

> **この文書はこの勤怠システムの「外せない（削除不可）」恒久仕様です。**
> 給与取込CSV／XLSX出力（「給与取込」「給与取込2」ボタン）のロジックは、ここに記載した労務士確定ルールに従うこと。
> 出典：顧問労務士からの回答（依頼受領：2026年6月）。変更時は労務士確認を必須とし、本文書を更新してから実装すること。

---

## 1. 時間外手当の集計ルール（A＝法定内残業／B＝法定外残業）

時間外は **A（法定内残業）** と **B（法定外残業）** の2区分で集計する。
判定は「日次」と「週次」の二段階で行う。

### 1-1. 日次の振り分け（基準：1日8時間を超えるか）

各日について、所定（契約）時間を超えた労働を次のように振り分ける。

- **A（法定内残業）** ＝ 所定時間を超え、かつ **1日8時間以内** の部分
- **B（法定外残業）** ＝ **1日8時間を超えた** 部分

計算式（その日の所定時間を `所定`、実労働を `実働` とする）：

```
A_日次 = max(0, min(実働, 8) − 所定)
B_日次 = max(0, 実働 − 8)
```

#### 例：木曜日の契約（所定）時間が4時間の場合
| 実労働 | A（法定内残業） | B（法定外残業） |
|--------|----------------|----------------|
| 5時間勤務 | A 1時間 | － |
| 9時間勤務 | A 4時間 | B 1時間 |

- 5時間：所定4hを1h超過、8h以内なので **A 1h**。
- 9時間：所定4h→8hまでの4hが **A**、8h超の1hが **B**。

### 1-2. 週次の上乗せ（基準：週合計40時間を超えるか）

日次の集計に加え、**週の合計労働時間が40時間を超えた分は「B」としてカウント**する。
すなわち、日次では「A（8時間以内）」と判定された残業であっても、その週の累計が40時間を超える部分は **B に振り替える**。

#### 例：月〜金で35時間労働し、土曜（所定4時間）に7時間労働した場合
1. 前5日（月〜金）の実績 35時間 に、土曜の所定 4時間 を足すと **39時間**。
2. 土曜の所定超過は 7−4＝**3時間**。1日の上限（8時間）以内だが、週40時間を基準に振り分ける。
3. 週40時間まで残り `40 − 39 = 1時間` → 土曜超過3hのうち **1hがA**。
4. 残り 2時間 は週40時間を超えるため **B**。

→ **土曜日の実績：A 1時間、B 2時間**

> ポイント：1日8時間以内であっても、週40時間超過分は B になる。週次判定が日次判定を上書きして B へ振り替える。

---

## 2. 回数手当（**今回は対象外**）

- 回数手当は **廃止**。**今回の給与計算・出力では無視する**（CSVの回数手当1〜3は空欄出力）。
- 代替の支給方法が将来決まった場合は、本文書に追記してから実装すること。

---

## 3. 非課税支給（交通費）

- 「非課税支給」項目は **主に時給職員の交通費** を指す。
- 雛形（テンプレート）に **「出勤日数」** を入力すると、各職員の **1日あたりの交通費単価** に基づき、**1ヶ月分の合計額を自動計算** する。

```
非課税支給（月額） = 交通費単価（1日あたり） × 出勤日数
```

---

## 4. 法定休日・日曜出勤の扱い

- 法定休日は **日曜日・祝日**（実装の `isLegalHoliday` も日曜 OR 祝日で一致）。
- 日曜日に出勤する場合、**同じ週の別日に振替休日** を取得すれば、その日曜は
  **振替出勤（通常の出勤と同条件）** として給与計算が可能。
  - 振替休日が同週に取得されている → 当該日曜は割増なしの通常出勤扱い。
  - 振替休日がない日曜出勤は、休日勤務（時間外C・35%）扱い。

## 4-2. 夏季・冬季休暇の有休消化

会社の一斉休業期間は、有給を「有休消化」として自動的に充当する。

| 休暇 | 期間 | 有休消化 | 適用日 |
|------|------|----------|--------|
| 夏季休暇 | 8/11（山の日）〜8/15 | **1日** | **8/15** |
| 冬季休暇 | 12/29 午後〜1/4 | **2日** | **12/31・1/2** |

- **【適用対象】この有休消化は「常勤」のみに適用する。**
  - 「常勤」＝有給付与モードが「自動・常勤」(`paidMode==='auto_full'`、週5日以上 or 週30h以上)のスタッフ。
  - 比例付与（`auto_part`）・手動（`manual`）・時給/バイト枠は対象外。
- 冬季の適用日は当初 12/31・1/1 だったが、**1/1は法定休日（元日・祝日）のため 1/2 に変更**。
- 有休消化日は通常の有給（全日）として扱う。
- 実装：`ensureSeasonalPaid()` が月次集計時に常勤スタッフへ全日有給を自動登録（idempotent・既存があればスキップ・法定休日には付与しない）。memo＝「夏季休暇(有休消化)」「冬季休暇(有休消化)」。

---

## 補足

- 締日運用：前月16日〜当月15日（雇用契約上の支払対象期間と整合）。
- 出力対象ボタン：個人/全体の「給与取込」「給与取込2」。
- 本ルールに反する出力が見つかった場合は、出力側を本文書に合わせて修正する。
